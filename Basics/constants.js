const interestRate = 0.3;
// interestRate = 1; Adding this will cause a error 
console.log(interestRate)

// There are times we have a variable value that we do not want to change.
// This is because it can cause a bug in the program
// To avoid this we use const.
// We can not reassign a value on a constant 
// Following best practices if you need to reassign use let 
// If you don't then use const
let g = 2;

// Relational
console.log(g < 0);
console.log(g <= 0); // greater than or equal too
console.log(g > 1)
console.log(g >= 1) // less than or equal too

// Equality
console.log(g === 1); // see if it is equal too
console.log(g !== 1); // check to see if it is not eqaul too